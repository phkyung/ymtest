# 크롤러 의존성 (로컬 실행용)
# 설치: pip install -r requirements.txt

# HTTP 요청
requests==2.32.3
httpx[http2]>=0.27.0

# HTML 파싱
beautifulsoup4==4.12.3

# ── Firestore 업로드는 GitHub Actions에서만 사용 ──
# upload.py 실행 시 별도 설치 필요:
#   pip install firebase-admin==6.6.0
